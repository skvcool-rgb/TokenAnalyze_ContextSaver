"""MCP surface for kos-memory v4.

Two entry points:
- mcp.cli: command-line subcommands invoked by slash commands (no MCP).
- mcp.server: stdio MCP server exposing recall_project_memory and remember_fact.
"""
